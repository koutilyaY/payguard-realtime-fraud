# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.log
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual env
.venv/
venv/

# Spark / Delta / checkpoints
delta/
_checkpoints*/
.spark_tmp/

# Real dataset (downloaded via scripts/download_data.py, not committed)
data/creditcard.csv

# Trained model artifacts + metrics (generated by make train)
mlruns/fraud_model.pkl
mlruns/real_data_metrics.json

# Secrets / configs
config.yaml
.env
.env.*
secrets.*

# MLflow tracking dir — regenerated by 'make train', not committed
mlruns/
