# Minimal dependency set for the MLOps loop (training, registry, drift, shadow
# serving, tests). This is what CI installs — it deliberately excludes the heavy
# streaming stack (pyspark, delta, kafka, airflow) that the loop does not need, so
# CI is fast. The full streaming requirements live in requirements.txt.
lightgbm>=4.3.0
scikit-learn>=1.4.0
mlflow>=2.13.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0
pyyaml>=6.0
fastapi>=0.110.0
httpx>=0.27.0
pytest>=8.0.0
# api.py imports these at module load (tests mock the servers, but the import
# must resolve). psycopg2-binary avoids needing libpq headers in CI.
redis>=5.0.0
psycopg2-binary>=2.9.0
